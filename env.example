# ═══════════════════════════════════════════════════════════════
# AEGIX - Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and configure your values
# NEVER commit .env files to version control!
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────
# Gateway Configuration (packages/gateway/.env)
# ─────────────────────────────────────────────────────────────────

# Server
PORT=3001

# Solana Network
SOLANA_NETWORK=mainnet-beta
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# For better performance, use Helius or other RPC provider:
# SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# PayAI Facilitator (no API key required)
FACILITATOR_URL=https://facilitator.payai.network
PAYAI_NETWORK=solana

# Inco Network (FHE Privacy Layer) - DEPRECATED, use Light Protocol
INCO_NETWORK_URL=https://lightning.inco.org
INCO_CONTRACT_ADDRESS=

# Pool key encryption secret (generate with: openssl rand -hex 32)
# REQUIRED for production! Used to encrypt/decrypt FHE pool private keys
INCO_BYTES_KEY=your-32-byte-hex-secret-here

# ─────────────────────────────────────────────────────────────────
# Light Protocol (ZK Compression) - Aegix 4.0
# ─────────────────────────────────────────────────────────────────

# Light Protocol RPC (Helius recommended for compression support)
# Get your API key at https://helius.dev
LIGHT_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# Session key encryption secret (generate with: openssl rand -hex 32)
SESSION_KEY_SECRET=your-32-byte-hex-secret-here

# ─────────────────────────────────────────────────────────────────
# Recovery Pool (Dedicated Fee Payer) - Aegix 4.0
# ─────────────────────────────────────────────────────────────────
# The Recovery Pool pays transaction fees and ATA rent for stealth 
# payments, breaking the on-chain link between your pool and burners.
# 
# If not set, a new keypair will be generated automatically.
# Fund the generated address with at least 0.01 SOL for gas fees.
#
# To generate manually: solana-keygen new --no-bip39-passphrase
RECOVERY_POOL_PRIVATE_KEY=

# ─────────────────────────────────────────────────────────────────
# Dashboard Configuration (packages/dashboard/.env.local)
# ─────────────────────────────────────────────────────────────────

# Gateway URL (for frontend to connect to backend)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:3001

# ═══════════════════════════════════════════════════════════════
# SECURITY NOTES:
# ═══════════════════════════════════════════════════════════════
# 
# ✓ Aegix is NON-CUSTODIAL - never stores user private keys
# ✓ Pool wallet keys are encrypted and stored locally only
# ✓ All sensitive data in packages/gateway/data/ is gitignored
# ✓ FHE encryption via Inco Network protects audit logs
#
# NEVER commit:
# - .env files
# - packages/gateway/data/ contents
# - Private keys or seed phrases
# ═══════════════════════════════════════════════════════════════

